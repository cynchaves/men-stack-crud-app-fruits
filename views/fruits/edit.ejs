<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit a Fruit</title>
  </head>
  <body>
    <a href="/">Home</a>
    <a href="/fruits">Browse All Fruits</a>
    <a href="/fruits/new"> Add a New Fruit</a>
    <h1>Edit <%= fruit.name %> </h1>
    <form action="/fruits/<%=fruit._id%>?_method=PUT" method="POST">
      <div>
        <label for="name">Name:</label>
        <input type="text" name="name" value="<%= fruit.name %>" id="name" />
      </div>
      <div>
        <label for="isReadyToEat">Ready to Eat?</label>
        <input type="checkbox" name="isReadyToEat" <% if (fruit.isReadyToEat) { %>checked<% } %> id="isReadyToEat" />
     </div>
      <button type="submit">Update Fruit</button>
    </form>
  </body>
</html>